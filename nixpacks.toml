[providers]
python = {}

[phases.setup]
nixPkgs = ["...", "python3", "playwright-driver"]

[phases.build]
cmds = ["pip install -r requirements.txt", "playwright install chromium"]

[start]
cmd = "python ets2_bot.py"
